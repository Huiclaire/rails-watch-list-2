<% @movies.each do |movie| %>
  <div>
    <img src="<%= movie.poster_url %>" alt="<%= movie.title %>">
    <h4><%= movie.title %></h4>
    <p><%= movie.overview.first(50)+"...." %></p>
  </div>
  <%= link_to 'Read All',  movie_path(movie) %>
<% end %>
