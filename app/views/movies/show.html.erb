<div>
  <img src="<%= @movie.poster_url %>" alt="<%= @movie.title %>">
  <img src="<%= @movie.backdrop_path %>" alt="<%= @movie.title %>">
  <h1><%= @movie.title %></h1>
  <p><%= @movie.release_date %></p>
  <br>
  <p><%= @movie.overview %></p>
  <p>Rating: <%= "⭐️" * @movie.rating %></p>
  <%= link_to 'Add to a list', lists_path %>
</div>
